fn parse_line(l: str) {
    for (row_i, space) in s.split(' ').enumerate() {
        if !s.is_empty() {}
    }
}

pub fn parse_world_state(world_string: str) {
    for (col_i, line) in world_string.split('\n').enumerate() {
        parse_line(line);
    }
}
